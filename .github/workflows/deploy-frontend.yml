name: Desplegar frontend en AWS

on:
  push:
    branches: [main]
    paths:
      - 'frontend/**'
      - '.github/workflows/deploy-frontend.yml'
  workflow_dispatch:

concurrency:
  group: despliegue-ec2
  cancel-in-progress: false

jobs:
  deploy:
    name: EC2
    runs-on: ubuntu-latest
    environment: produccion
    steps:
      - name: Desplegar por SSH
        uses: appleboy/ssh-action@v1.0.3
        env:
          API_URL: ${{ vars.NEXT_PUBLIC_API_URL }}
        with:
          host: ${{ secrets.EC2_HOST }}
          username: ${{ secrets.EC2_USER }}
          key: ${{ secrets.EC2_SSH_KEY }}
          envs: API_URL
          script_stop: true
          script: |
            set -euo pipefail

            cd ~/Ordenix
            git fetch --all --prune
            git checkout main
            git reset --hard origin/main

            cd frontend
            npm ci

            if [ -z "${API_URL:-}" ]; then
              echo "Falta la variable NEXT_PUBLIC_API_URL del repositorio."
              echo "Definela en Settings > Secrets and variables > Actions > Variables."
              exit 1
            fi

            printf "NEXT_PUBLIC_API_URL=%s\n" "$API_URL" > .env.production

            npm run build

            pm2 restart frontend-ordenix --update-env || pm2 start npm --name frontend-ordenix -- run start
            pm2 save

            for intento in $(seq 1 10); do
              if curl -fsS "http://127.0.0.1:3000" > /dev/null; then
                echo "Frontend saludable."
                exit 0
              fi
              echo "Esperando al frontend ($intento/10)..."
              sleep 3
            done

            echo "El frontend no respondio tras el despliegue."
            pm2 logs frontend-ordenix --lines 40 --nostream
            exit 1
